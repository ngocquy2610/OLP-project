<p><strong><%= t('missing_i18n.payment_mailer.student_receipt.title', id: @order.id) %></strong></p>
<p><%= t('missing_i18n.payment_mailer.student_receipt.purchased_courses') %></p>
<ul>
	<% @courses.each do |c| %>
		<li><%= c.name %></li>
	<% end %>
</ul>
<p><strong><%= t('missing_i18n.payment_mailer.student_receipt.total_paid_label') %></strong> <%= format_vnd(@amount) %></p>